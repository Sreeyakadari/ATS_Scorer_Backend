# ATS Resume Scorer — combined backend + frontend dependencies.
#
# Install with:
#     pip install -r requirements.txt
#
# After installing, download the spaCy model the backend uses at startup:
#     python -m spacy download en_core_web_md
#
# WeasyPrint (used for PDF export) also needs system libraries on Linux:
#     sudo dnf install -y cairo pango gdk-pixbuf2 libffi    # Fedora
#     sudo apt install -y libcairo2 libpango-1.0-0 libpangoft2-1.0-0 libffi-dev  # Debian/Ubuntu

# ── Backend: web framework ──
# fastapi>=0.110
# uvicorn[standard]>=0.27
# pydantic>=2.6
# python-multipart>=0.0.9      # required by FastAPI for File/Form uploads
# python-dotenv>=1.0

# ── Backend: NLP / ML ──
# spacy>=3.7,<4.0
# sentence-transformers>=2.5
# numpy>=1.26
# rapidfuzz>=3.6

# ── Backend: file parsing ──
# pdfplumber>=0.11
# PyPDF2>=3.0
# python-docx>=1.1
# python-magic>=0.4.27

# ── Backend: PDF report generation ──
# jinja2>=3.1
# weasyprint>=60.0

# ── Backend: external services ──
# httpx>=0.27                  # async Supabase REST client
# groq>=0.13                   # Groq LLM client (0.13+ is required for httpx>=0.28 compatibility)
# PyJWT[crypto]>=2.8           # verifies Supabase access tokens (HS256)

# ── Frontend ──
# streamlit>=1.32
# requests>=2.31
# supabase>=2.4                # frontend auth client (email/password + OAuth)


# ATS Resume Scorer — combined backend + frontend dependencies.
#
# Install with:
#     pip install -r requirements.txt
#
# NOTE:
# WeasyPrint on Windows still requires MSYS2 GTK/Pango/Cairo libraries.
# Installing this file alone is NOT enough for WeasyPrint.
#
# If deploying on Windows, make sure:
# C:\msys64\ucrt64\bin is added to PATH.
#

# ==========================================================
# Backend: Web Framework
# ==========================================================

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9
python-dotenv>=1.0

# ==========================================================
# Backend: NLP / ML
# ==========================================================

spacy>=3.7,<4.0
sentence-transformers>=2.5
numpy>=1.26
rapidfuzz>=3.6

# SpaCy language models
# Automatically installed with pip install -r requirements.txt

https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

https://github.com/explosion/spacy-models/releases/download/en_core_web_md-3.8.0/en_core_web_md-3.8.0-py3-none-any.whl

# ==========================================================
# Backend: File Parsing
# ==========================================================

pdfplumber>=0.11
PyPDF2>=3.0
python-docx>=1.1

# Windows
python-magic-bin>=0.4.14; platform_system=="Windows"

# Linux / macOS
python-magic>=0.4.27; platform_system!="Windows"

# ==========================================================
# Backend: PDF Report Generation
# ==========================================================

jinja2>=3.1
weasyprint>=68.0

# Optional fallback PDF generation
reportlab>=4.2
beautifulsoup4>=4.12

# ==========================================================
# Backend: External Services
# ==========================================================

httpx>=0.27
groq>=0.13
PyJWT[crypto]>=2.8

# ==========================================================
# Database / Authentication
# ==========================================================

supabase>=2.4

# ==========================================================
# Frontend
# ==========================================================

streamlit>=1.32
requests>=2.31

# ==========================================================
# Utilities
# ==========================================================

Pillow>=10.0
typing-extensions>=4.10